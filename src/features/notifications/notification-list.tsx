import Link from "next/link";
import { AlertTriangle, BellRing, Info } from "lucide-react";
import type { NotificationItem } from "@/features/sam/types";
const config = { critical: { icon: AlertTriangle, style: "bg-red-50 text-red-700" }, warning: { icon: BellRing, style: "bg-amber-50 text-amber-700" }, info: { icon: Info, style: "bg-blue-50 text-blue-700" } };
export function NotificationList({ notifications }: { notifications: NotificationItem[] }) { return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="divide-y divide-slate-100">{notifications.map((item) => { const Icon = config[item.severity].icon; return <Link key={item.id} href={`/${item.entityType === "asset" ? "assets" : "licenses"}/${item.entityId}`} className="flex gap-4 p-5 hover:bg-slate-50"><span className={`grid size-11 shrink-0 place-items-center rounded-xl ${config[item.severity].style}`}><Icon aria-hidden="true" size={20} /></span><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="font-bold text-slate-950">{item.title}</h2>{!item.read ? <span className="size-2 rounded-full bg-blue-600" aria-label="ยังไม่อ่าน" /> : null}</div><p className="mt-1 text-sm text-slate-500">{item.message}</p><p className="mt-2 text-xs font-medium text-slate-400">{item.createdAt.replace("T", " ").slice(0, 16)} UTC</p></div></Link>; })}</div></section>; }
