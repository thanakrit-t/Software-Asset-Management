import type { Role } from "@/features/sam/types";
import { AccessDenied } from "./access-denied";
const sections = [{ name: "Sites", items: ["TKC Factory", "Bangkok Office"] }, { name: "Asset Types", items: ["PC", "Notebook", "Server", "Other"] }, { name: "Asset Statuses", items: ["Active", "Spare", "Repair", "Retired"] }, { name: "License Types", items: ["Device", "Named User", "Subscription", "Perpetual"] }, { name: "Software Categories", items: ["Operating System", "Office", "Database", "CAD", "Utility"] }, { name: "Internet Levels", items: ["Level 1", "Level 2", "Level 3", "Internal"] }];
export function MasterDataView({ role }: { role: Role }) { if (role !== "admin") return <AccessDenied />; return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{sections.map((section) => <section key={section.name} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-bold text-slate-950">{section.name}</h2><button className="text-xs font-bold text-blue-700">+ เพิ่มค่า</button></div><ul className="mt-4 divide-y divide-slate-100">{section.items.map((item) => <li key={item} className="flex justify-between py-3 text-sm text-slate-700"><span>{item}</span><span className="text-xs text-slate-400">Active</span></li>)}</ul></section>)}</div>; }
