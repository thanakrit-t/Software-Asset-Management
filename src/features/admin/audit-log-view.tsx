import type { AuditEvent, Role } from "@/features/sam/types";
import { ScrollText } from "lucide-react";
import { AccessDenied } from "./access-denied";
export function AuditLogView({ role, events }: { role: Role; events: AuditEvent[] }) { if (role !== "admin") return <AccessDenied />; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="space-y-3">{events.map((event) => <article key={event.id} className="flex gap-3 rounded-xl border border-slate-100 p-4"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-slate-100 text-slate-600"><ScrollText aria-hidden="true" size={18} /></span><div><p className="text-sm font-bold text-slate-900">{event.description}</p><p className="mt-1 text-xs text-slate-500">{event.actor} · {event.action} · {event.entityType} #{event.entityId}</p><p className="mt-1 text-[11px] text-slate-400">{event.occurredAt.replace("T", " ").slice(0, 16)} UTC</p></div></article>)}</div></section>; }
