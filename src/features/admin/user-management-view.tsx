import type { Role, UserAccount } from "@/features/sam/types";
import { TableShell } from "@/components/data-table/table-shell";
import { StatusBadge } from "@/components/ui/status-badge";
import { AccessDenied } from "./access-denied";
export function UserManagementView({ role, users }: { role: Role; users: UserAccount[] }) { if (role !== "admin") return <AccessDenied />; return <TableShell><table className="w-full min-w-[750px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Name", "Email", "Role", "Status", "Last login"].map((item) => <th key={item} className="border-b border-slate-200 px-5 py-3.5">{item}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{users.map((user) => <tr key={user.id}><td className="px-5 py-4 font-bold text-slate-900">{user.name}</td><td className="px-5 py-4 text-slate-600">{user.email}</td><td className="px-5 py-4 capitalize">{user.role}</td><td className="px-5 py-4"><StatusBadge status={user.status === "active" ? "active" : "retired"} label={user.status} /></td><td className="px-5 py-4 text-slate-500">{user.lastLoginAt?.slice(0, 10) ?? "—"}</td></tr>)}</tbody></table></TableShell>; }
